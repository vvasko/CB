<%= page_header 'Cocktails', ' ' <<  (@cocktails.blank? ? 'are not available' : '') %>

<% @cocktails.each do |type, cocktails| %>
    <%= render partial: 'group', locals: {cocktails: cocktails, type: type} %>
<% end %>